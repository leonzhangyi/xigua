<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Base application theme.   Theme.AppCompat.Light.DarkActionBar -->
    <style name="BaseAppTheme" parent="Theme.AppCompat">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowBackground">@color/white</item>
    </style>

    <!-- Title样式 -->
    <style name="title_style" parent="ToolbarTheme">
        <item name="actionOverflowMenuStyle">@style/OverflowMenuStyle</item>
    </style>

    <style name="ToolbarTheme" parent="@style/Widget.AppCompat.ActionBar">
        <item name="colorControlNormal">?attr/colorPrimary</item><!--toolbar所有按钮颜色-->
        <item name="titleTextColor">@android:color/white</item><!--标题颜色-->
        <item name="android:background">@color/black</item><!--toolbar背景颜色-->
        <!--需要更换menu按钮图标放开下面的注释-->
        <!--<item name="android:actionOverflowButtonStyle">@style/OverflowButtonStyle</item>-->
    </style>

    <!--修改toolbar弹出菜单的位置-->
    <style name="OverflowMenuStyle" parent="Widget.AppCompat.PopupMenu.Overflow">
        <item name="overlapAnchor">false</item>  <!--把该属性改为false即可使menu位置位于toolbar之下-->
        <item name="android:popupBackground">@color/colorPrimary</item>
    </style>
    <!-- Title样式 End-->
    <!-- 加载 dialog样式 -->
    <style name="DialogStyle_Loading" parent="Theme.AppCompat.Dialog">
        <!--背景透明-->
        <item name="android:windowBackground">@color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="colorAccent">@color/colorPrimary</item>
    </style>

    <!--开机页-->
    <style name="SplaceActivityStyle" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:background">@drawable/splash_bg</item>
    </style>

    <style name="navigation_bottom_radio">

        <!-- 内部组件的排列 -->
        <item name="android:gravity">center</item>
        <!-- 宽度 -->
        <item name="android:layout_width">0dp</item>
        <!-- 高度 -->
        <item name="android:layout_height">wrap_content</item>
        <!-- 设置RadioButton的原来图片为空 -->
        <item name="android:button">@null</item>
        <!-- 与其他组件宽度占相同比重 -->
        <item name="android:layout_weight">1.0</item>
        <!-- 顶部的空隙 -->
        <item name="android:paddingTop">9.0dip</item>
        <!-- 文字的大小 -->
        <item name="android:textSize">10sp</item>
        <!-- 文字的颜色 -->
        <item name="android:textColor">@drawable/main_text_color</item>
        <!-- 文字的加粗 -->
        <item name="android:textStyle">bold</item>
    </style>


    <style name="MyProgressBar" parent="Base.Widget.AppCompat.ProgressBar"></style>


    <!-- 消息对话弹框Dialog -->
    <style name="message_button_dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- 边框 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">false</item>
        <!-- 半透明 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 无标题 -->
        <item name="android:windowBackground">@color/transparent</item>
        <!-- 背景透明 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 外背景模糊 -->
        <item name="android:backgroundDimAmount">0.7</item>
    </style>

    <style name="download_edit_style">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">@dimen/size_dp_35</item>
        <item name="android:layout_weight">1</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@drawable/btn_grayee_main_color_2</item>
    </style>

    <!-- 自定义对话框样式 -->
    <style name="remindDialog" parent="android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowFrame">@null</item>
    </style>

</resources>
